import ClientPage from "./ClientPage";

export const metadata = {
  title: "Help & Guidance | Audito",
  description: "Role-based instructions for using Audito.",
};

export default function HelpSettingsPage() {
  return <ClientPage />;
}
